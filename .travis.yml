dist: trusty
language: bash
script:
  - cd src
  - zip -r ../IranianPersets.zip *

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_DEPLOY_TOKEN
  keep-history: true
  on:
    branch: master